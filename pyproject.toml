[project]
name = "agent-ui"
version = "0.1.0"
description = "Mobile-first web UI for controlling Claude Code agent sessions."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "python-dotenv>=1.0.1",
    "uvicorn[standard]>=0.30.0",
]

[tool.uv]
package = false
